# Data Contracts

- User state includes seed, step, image path, progression timestamp, ticket count, save slot limits.
- Saved image records include run metadata and timestamp.
- Deletion behavior must be deterministic and auditable.

## Execution Note
Operational HOW details are maintained in `skills/backend_api/SKILL.md`.
